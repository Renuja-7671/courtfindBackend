<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>CourtFind - Invoice</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        font-size: 20px;
        background-color: #f8f9fa;
        margin: 0;
        padding: 0;
        color: #050505;
        position: relative;
      }

      .invoice-container {
        position: relative;
        background: #fff;
        width: 700px;
        margin: 40px auto;
        padding: 30px 40px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        border-radius: 12px;
        position: relative;
        z-index: 1;
      }

      .header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 30px;
      }

      .header img {
        width: 140px;
        height: auto;
      }

      .header h2 {
        margin: 0;
        color: #002794;
        font-size: 28px;
      }

      .details p {
        margin: 8px 0;
        font-size: 20px;
      }

      .details strong {
        color: #19333d;
      }

      .divider {
        border-top: 2px solid #ccc;
        margin: 20px 0;
      }

      .footer {
        text-align: center;
        margin-top: 30px;
        font-size: 16px;
        color: #777;
      }

      .watermark {
        position: absolute;
        top: 40%;
        left: 50%;
        transform: translate(-50%, -50%) rotate(-30deg);
        font-size: 60px;
        color: rgba(0, 0, 0, 0.348);
        font-weight: bold;
        z-index: 0;
        pointer-events: none;
      }

      .paid-stamp {
        position: absolute;
        bottom: 100px; /* Distance from bottom */
        left: 50%;
        transform: translateX(-50%) rotate(-20deg);
        font-size: 60px;
        color: #e10404;
        border: 4px solid #6e0000;
        padding: 10px 25px;
        border-radius: 8px;
        font-weight: bold;
        opacity: 0.6;
        z-index: 5;
      }
    </style>
  </head>
  <body>
    <div class="watermark">CourtFind</div>

    <div class="invoice-container">

      <div class="header">
        <img
          src="http://localhost:8000/uploads/assets/logoOnly.png"
          alt="CourtFind Logo"
        />
        <h2>Arena Adding Invoice</h2>
      </div>

      <div class="divider"></div>

      <div class="details">
        <p><strong>Arena ID:</strong> <%= arena.arenaId %></p>
        <p><strong>Arena Name:</strong> <%= arena.name %></p>
        <p><strong>Owner ID:</strong> <%= arena.owner_id %></p>
        <p><strong>Paid Amount:</strong> Rs. <%= arena.amount %></p>
        <p><strong>Status:</strong> Payment Received Successfully</p>
        <p><strong>Invoice Date:</strong> <%= new Date().toLocaleDateString() %></p>
      </div>

      <div class="paid-stamp">PAID</div>

      <div class="divider"></div>

      <div class="footer">
        &copy; <%= new Date().getFullYear() %> CourtFind. All rights reserved.
      </div>
    </div>
  </body>
</html>
